"use strict";

function countIngots(report) {
    return 1;
}

var assert = require('assert');

if (!global.is_checking) {
    // These "asserts" using only for self-checking and not necessary for auto-testing
    assert.equal(count_ingots("A2,B1"), 12, "One and eleven");
    assert.equal(count_ingots("A1,A1,A1"), 3, "One, two, three");
    assert.equal(count_ingots("Z9,X8,Y7"), 672, "XYZ");
    assert.equal(count_ingots("C1,D1,B1,E1,F1"), 140, "Daily");
}
